package com.sanskar.hashurl.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.sanskar.hashurl.entity.UrlHash;

public interface UrlRepository extends JpaRepository<UrlHash, Long>{
	UrlHash findByHash(String hash);
}
